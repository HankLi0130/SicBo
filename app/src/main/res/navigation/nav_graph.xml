<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/roundFragment">

    <fragment
        android:id="@+id/roundFragment"
        android:name="dev.hankli.sicbo.round.RoundFragment"
        android:label="RoundFragment"
        tools:layout="@layout/fragment_round">
        <action
            android:id="@+id/action_roundFragment_to_editRoundFragment"
            app:destination="@id/editRoundFragment" />
    </fragment>

    <fragment
        android:id="@+id/editRoundFragment"
        android:name="dev.hankli.sicbo.round.EditRoundFragment"
        android:label="EditRoundFragment"
        tools:layout="@layout/fragment_edit_round" />

</navigation>